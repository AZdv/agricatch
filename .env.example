# Copy to .env and fill in. Never commit the real file.
DJANGO_SECRET_KEY=
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Defaults to sqlite at BASE_DIR/agricatch.sqlite3 when unset.
DATABASE_URL=
